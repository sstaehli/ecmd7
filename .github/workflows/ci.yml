name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  PROJECT_NAME: ecmd7
  OUTPUT_DIR: ecmd7-out

jobs:
  build:
    name: Build Hardware Files
    runs-on: ubuntu-latest
    
    container:
      image: kicad/kicad:9.0-full
      options: --user root
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Generate KiCad exports
        run: |
          echo "generating Gerber files for ${PROJECT_NAME}..."
          mkdir -p "${OUTPUT_DIR}/gerbers"
          kicad-cli pcb export gerbers "kicad/${PROJECT_NAME}.kicad_pcb" \
            --format GerberX2 \
            --units mm \
            --output "${OUTPUT_DIR}/gerbers"
          kicad-cli pcb export drill "kicad/${PROJECT_NAME}.kicad_pcb" \
            --format Excellon \
            --units mm \
            --output "${OUTPUT_DIR}/gerbers"
          echo "Generating documentation for ${PROJECT_NAME}..."
          mkdir -p "${OUTPUT_DIR}/schematic"
          kicad-cli sch export pdf "kicad/${PROJECT_NAME}.kicad_sch" \
            -o "${OUTPUT_DIR}/schematic/${PROJECT_NAME}_schematic.pdf"
          mkdir -p "${OUTPUT_DIR}/assembly"
          kicad-cli pcb export pdf "kicad/${PROJECT_NAME}.kicad_pcb" \
            --layers F.Fab,F.Paste \
            -o "${OUTPUT_DIR}/assembly/${PROJECT_NAME}_assembly.pdf"
          echo "Generating 3D image for ${PROJECT_NAME}..."
          mkdir -p "${OUTPUT_DIR}/render"
          kicad-cli pcb render "kicad/${PROJECT_NAME}.kicad_pcb" \
            --width 1920 --height 1080 \
            --background transparent \
            --perspective \
            --rotate 315,0,350 \
            --pan 0,1,0 \
            --zoom 1.2 \
            -o ${OUTPUT_DIR}/render/${PROJECT_NAME}_3d.png
          kicad-cli pcb render "kicad/${PROJECT_NAME}.kicad_pcb" \
            --width 1920 --height 1080 \
            --background transparent \
            --rotate 0,0,0 \
            --zoom 1.2 \
            -o ${OUTPUT_DIR}/render/${PROJECT_NAME}_2d.png
          echo "Creating zip artifact ${PROJECT_NAME}-out.zip"
          zip -r "${PROJECT_NAME}-out.zip" "${OUTPUT_DIR}"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ecmd7-out
          path: |
            ${{ env.OUTPUT_DIR }}
            ${{ env.PROJECT_NAME }}-out.zip

      - name: Release zip package
        uses: softprops/action-gh-release@v1
        if: github.ref_type == 'tag'
        with:
          files: ${{ env.PROJECT_NAME }}-out.zip

  pages:
    name: Build Hugo site
    if: github.ref_type == 'tag'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: ecmd7-out
          path: .

      - name: Prepare Hugo static files
        run: |
          mkdir -p doc/hugo/static/files/
          if [ -f "${{ env.PROJECT_NAME }}-out.zip" ]; then unzip -o "${{ env.PROJECT_NAME }}-out.zip"; fi
          mv "${OUTPUT_DIR}/schematic/${PROJECT_NAME}_schematic.pdf" doc/hugo/static/files/schematic.pdf || true
          mv "${OUTPUT_DIR}/assembly/${PROJECT_NAME}_assembly.pdf" doc/hugo/static/files/assembly.pdf || true
          mv "${OUTPUT_DIR}/render/${PROJECT_NAME}_2d.png" doc/hugo/static/files/2d.png || true
          mv "${OUTPUT_DIR}/render/${PROJECT_NAME}_3d.png" doc/hugo/static/files/3d.png || true

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'

      - name: Build site
        run: |
          cd doc/hugo
          hugo --baseURL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/" --minify

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: doc/hugo/public
  
  deploy:
    name: Deploy Hugo site
    if: github.ref_type == 'tag'
    needs: pages
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
  
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
